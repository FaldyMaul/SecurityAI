/** Placeholder: RadarChart wrapper — recharts RadarChart, lazy-loaded */
export function RadarChartWrapper() {
  return <div style={{ padding: 'var(--space-8)', textAlign: 'center', color: 'var(--color-text-muted)', fontSize: 'var(--text-sm)', border: '1px dashed var(--color-border)', borderRadius: 'var(--radius-lg)' }}>📊 RadarChart placeholder — will use recharts RadarChart with overlaid model scores. Lazy-loaded.</div>;
}
