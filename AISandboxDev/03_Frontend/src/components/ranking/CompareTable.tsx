/** Placeholder: CompareTable — side-by-side model attributes */
export function CompareTable() {
  return <div style={{ padding: 'var(--space-4)', color: 'var(--color-text-muted)', fontSize: 'var(--text-sm)' }}>🔧 CompareTable placeholder — side-by-side model comparison with winning score highlighting.</div>;
}
